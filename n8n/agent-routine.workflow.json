{
  "name": "Lab3 — Agent Routine (template)",
  "nodes": [
    {
      "parameters": {
        "rule": {
          "interval": [
            { "field": "cronExpression", "expression": "0 7 * * 1-5" }
          ]
        }
      },
      "id": "schedule-trigger",
      "name": "Every weekday 07:00",
      "type": "n8n-nodes-base.scheduleTrigger",
      "typeVersion": 1.2,
      "position": [240, 300]
    },
    {
      "parameters": {
        "command": "=cd /path/to/lab3-agentic-capgemini && npm run fetch:news -- \"GenAI enterprise\""
      },
      "id": "run-agent-step",
      "name": "Run agent step",
      "type": "n8n-nodes-base.executeCommand",
      "typeVersion": 1,
      "position": [520, 300],
      "notes": "ADAPT ME: this is where your agent / script runs. Point it at the repo and the command for your project (fetch:news, build:deck, or a Claude Code headless run)."
    },
    {
      "parameters": {
        "options": {}
      },
      "id": "deliver",
      "name": "Deliver (adapt: email / Slack / file)",
      "type": "n8n-nodes-base.noOp",
      "typeVersion": 1,
      "position": [800, 300],
      "notes": "ADAPT ME: replace with an Email / Slack / Write-to-file node to deliver the result."
    }
  ],
  "connections": {
    "Every weekday 07:00": {
      "main": [[{ "node": "Run agent step", "type": "main", "index": 0 }]]
    },
    "Run agent step": {
      "main": [[{ "node": "Deliver (adapt: email / Slack / file)", "type": "main", "index": 0 }]]
    }
  },
  "settings": {},
  "meta": {
    "templatecredsSetupCompleted": false
  },
  "pinData": {}
}
